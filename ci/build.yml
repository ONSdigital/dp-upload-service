---

platform: linux

image_resource:
  type: docker-image
  source:
    repository: golang
    tag: 1.26.3-bookworm

inputs:
  - name: dp-upload-service

outputs:
  - name: build

caches:
  - path: go/

run:
  path: dp-upload-service/ci/scripts/build.sh
